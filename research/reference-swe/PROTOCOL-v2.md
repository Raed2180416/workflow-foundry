# Reference-02: frozen, diagnostically assisted construction

This revision preserves reference-01, its provider interruption, and its failed
source-stability gate. The study-only generic overlay was approved by prime on
2026-09-13. It is additional diagnostic assistance, not a held-out skills trial.
No previous task solutions, hidden cases, or actual executor replies are supplied
to the architect. The two original task definitions, public/hidden cases,
executor system prompt, operation parser and oracle remain unchanged.

Run only the copied reference-02 entrypoint after copying and hashing the full
Foundry source/skills/schema, installed Node dependencies, the pinned Python
environment, reviewed mini-swe-agent source and license, and study/test code.
Copy real bytes, not links back to mutable project dependencies. Inventory and
recheck all copied files and internal symlinks. Record and recheck external
system binaries/interpreter/libraries separately; they are host dependencies,
not a hermetic operating-system image. Reject closure or host-runtime drift.
Live project changes after freeze are informational and do not change this run.

Use actual WorkflowGenerator with selected frozen installed skills and the
unchanged custom reference.query/reference.act catalog. Add the approved rule
to its generic request. No evaluator is passed to the generator, so its proposals
remain unqualified. At most TWO architect provider invocations total, each with
maxRounds=1. A second invocation may see the prior candidate and generic schema
or neutral conformance diagnostics; never task solutions or hidden cases.
Do not hand-patch generated candidates. Save every attempt before assessing it.

The local gate emits status=partial, constructionPassed, deploymentQualified=false
and taskQualified=false. Neutral scripted transcripts exercise immediate finish,
three turns, invalid-operation feedback, finish on the fifth turn, and five
nonfinishing turns. The exact candidate must preserve histories, execute one
operation per response, and stop according to actual submission. Binding and
stopping mutations must be rejected. These controls use zero model calls and
are reported separately from actual task outcomes. They contain no repair code.

Freeze the accepted candidate bytes before actual task arms. Both arms use the
same existing OpenCodeFreeProvider, exact opencode/ling-3.0-flash-fin-free model,
150-second provider ceiling, identical initial prompts and narrow operations,
five executor calls maximum, and 300 seconds per task/arm. No paid fallback,
extra executor retries, hidden whole-agent capability or human task assistance.
Overall new revision work is bounded by the explicitly recorded deadline.
Task/arm order remains bucket reference/Foundry then last-occurrence Foundry/reference.

Original DefaultAgent is imported unchanged from the frozen reviewed upstream
revision through its documented custom Model/Environment interfaces. Original
run/step/query/history/stopping/serialization execute in no-network bubblewrap
with an empty MSWEA_GLOBAL_CONFIG_DIR and no host-home or credential environment.
Candidate Python runs only through the original fixed no-network bubblewrap
oracle: 2 CPU seconds, 4 wall seconds, 256 MiB, 64 KiB captured output, read-only
candidate/oracle, fixed immutable tests and external expected outputs. Original
source/config and adapter differences remain as documented in protocol v1.

A successful task requires explicit submission, successful original/native
completion, complete verified model identity/cost exports, unchanged evidence,
and every fixed hidden case passing including input nonmutation. Record unavailable
provider, malformed provider transport, sandbox/oracle failure, construction
failure and executor failure separately. An incomplete arm is not a model loss.
Do not turn two tasks into a product-parity, SWE-bench, TUI or reliability claim.
Seal new receipts without modifying reference-01 or its old failed receipts.
