# Baseline — 2026-09-13

Research begins in `/home/raed/Projects/workflow-foundry`, a new independent Git
repository. Existing ARC is inspected, not rewritten or repaired incidentally.

Observed host: Node 24.20.0, Python 3.14.7, OpenCode 1.18.29; approximately 4.5 GiB
available RAM and 384 GiB free disk at initial inspection. No model download or
mass runtime installation is required for corpus acquisition.

Existing `.agentic-os` HEAD: `4059ac32606acadf22e7c9e21480a6aaf48c65dc`, branch
`codex/semantic-v2-product-splice-20260811`. Existing changes: `.serena/project.yml`
and an untracked hippocampus finding. Do not touch either.

Initial truth probe reports incomplete composition and unavailable local model/demo
services. Holt strict read-only commands fail because its installed trusted tool
imports a missing `src/discover.mjs`. This is an unavailable integrity probe, not
permission to repair/re-sign the protected broker or clean worktrees. Canon check
fails on the pre-existing untracked finding; hygiene also reports three unclassified
ignored directories. These are baseline observations, not defects introduced here.

Earlier conversation architecture assigning AMP the generic workflow-compiler role
is a proposal, not verified implementation. Actual AMP integration must preserve
its existing semantic/context contract; the foundry owns its own workflow IR.
